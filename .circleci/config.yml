version: 2.1
jobs:
  build:
    docker:
      - image: docker:23.0.5-cli-alpine3.17
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: Run tests
          command: |
            docker-compose up -d
            docker-compose run web rspec
workflows:
  backend:
    jobs:
      - build