version: 2.1
jobs:
  build:
    working_directory: /stopover
    docker:
      - image: cimg/base:2023.05
    steps:
      - checkout
      - run:
          name: Run tests
          command: |
            docker-compose up -d
            docker-compose run stopover-web rspec
workflows:
  backend:
    jobs:
      - build