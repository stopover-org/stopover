version: 2.1
jobs:
  build:
    working_directory: /stopover
    steps:
      - checkout
      - run:
          name: Run tests
          command: |
            docker-compose up -d
            docker-compose run stopover-web rspec
workflows:
  backend:
    jobs:
      - build