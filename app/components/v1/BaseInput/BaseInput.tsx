import styled from "styled-components";
import React from "react";

const SInput = styled.input`
  width: 100%;
  font-weight: 300;
  font-family: "Roboto";
  font-size: 18px;
`;

export default React.memo(SInput);
