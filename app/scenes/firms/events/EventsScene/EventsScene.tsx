import React from "react";

interface EventsSceneProps {}

const EventsScene = (props: EventsSceneProps) => {
  console.log("don't collapse me");
  return null;
};

export default React.memo(EventsScene);
