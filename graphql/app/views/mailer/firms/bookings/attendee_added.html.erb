<h3>New Attendee was added to the <a href="<%= Rails.application.credentials.frontend_url %>/my-firm/bookings/<%= GraphqlSchema.id_from_object(booking)%>">booking</a></h3>
Attendee was added by <%= user.email %>
